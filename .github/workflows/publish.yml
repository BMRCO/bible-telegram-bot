name: Publication automatique

on:
  schedule:
    # Cron em UTC. França = UTC+2 (été) ou UTC+1 (hiver)
    # 🌅 5h07 UTC → 7h07 FR  | Protection Divine (image)
    # ☀️ 7h07 UTC → 9h07 FR  | Promesse / Espérance (reel)
    # ✝️ 10h07 UTC → 12h07 FR | Parabole de Jésus (DIMANCHE seulement)
    # 🍽️ 11h07 UTC → 13h07 FR | Sagesse / Amour (image)
    # 🌤️ 13h07 UTC → 15h07 FR | Jésus / Prière (reel)
    # 🌇 17h07 UTC → 19h07 FR | Prophétie / Paix (image)
    # 🌙 19h07 UTC → 21h07 FR | Psaume du Soir (reel)
    - cron: '7 5 * * *'
    - cron: '7 7 * * *'
    - cron: '7 10 * * 0'    # ✝️ Dimanche 10h07 UTC (12h07 FR) — Parabole de Jésus
    - cron: '7 11 * * *'
    - cron: '7 13 * * *'
    - cron: '7 17 * * *'
    - cron: '7 19 * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Mode de publication'
        required: true
        type: choice
        default: 'auto'
        options:
          - auto
          - image
          - reel
          - parabole
      category:
        description: 'Catégorie (laisser vide pour rotation auto)'
        required: false
        type: choice
        default: ''
        options:
          - ''
          - promise
          - jesus
          - psaume
          - proverbe
          - prophetie
          - protection
          - esperance
          - paix
          - amour
          - priere

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pillow requests numpy cloudinary google-api-python-client google-auth-httplib2 google-auth-oauthlib

      - name: Install ffmpeg
        uses: FedericoCarboni/setup-ffmpeg@v3
        with:
          ffmpeg-version: release

      - name: Determine publication mode and category
        id: schedule
        run: |
          SCHEDULE="${{ github.event.schedule }}"
          MANUAL_MODE="${{ inputs.mode }}"
          MANUAL_CAT="${{ inputs.category }}"

          if [ -n "$SCHEDULE" ]; then
            case "$SCHEDULE" in
              "7 5 * * *")
                echo "mode=image" >> $GITHUB_OUTPUT
                echo "category=protection" >> $GITHUB_OUTPUT
                echo "🌅 Cron 5h07 UTC → image protection"
                ;;
              "7 7 * * *")
                echo "mode=reel" >> $GITHUB_OUTPUT
                if [ $(($(date +%j) % 2)) -eq 0 ]; then
                  echo "category=promise" >> $GITHUB_OUTPUT
                  echo "☀️ Cron 7h07 UTC → reel promise"
                else
                  echo "category=esperance" >> $GITHUB_OUTPUT
                  echo "☀️ Cron 7h07 UTC → reel esperance"
                fi
                ;;
              "7 11 * * *")
                echo "mode=image" >> $GITHUB_OUTPUT
                if [ $(($(date +%j) % 2)) -eq 0 ]; then
                  echo "category=proverbe" >> $GITHUB_OUTPUT
                  echo "🍽️ Cron 11h07 UTC → image proverbe"
                else
                  echo "category=amour" >> $GITHUB_OUTPUT
                  echo "🍽️ Cron 11h07 UTC → image amour"
                fi
                ;;
              "7 13 * * *")
                echo "mode=reel" >> $GITHUB_OUTPUT
                if [ $(($(date +%j) % 2)) -eq 0 ]; then
                  echo "category=jesus" >> $GITHUB_OUTPUT
                  echo "🌤️ Cron 13h07 UTC → reel jesus"
                else
                  echo "category=priere" >> $GITHUB_OUTPUT
                  echo "🌤️ Cron 13h07 UTC → reel priere"
                fi
                ;;
              "7 17 * * *")
                echo "mode=image" >> $GITHUB_OUTPUT
                if [ $(($(date +%j) % 2)) -eq 0 ]; then
                  echo "category=prophetie" >> $GITHUB_OUTPUT
                  echo "🌇 Cron 17h07 UTC → image prophetie"
                else
                  echo "category=paix" >> $GITHUB_OUTPUT
                  echo "🌇 Cron 17h07 UTC → image paix"
                fi
                ;;
              "7 10 * * 0")
                echo "mode=parabole" >> $GITHUB_OUTPUT
                echo "category=jesus" >> $GITHUB_OUTPUT
                echo "✝️ Cron Dimanche 10h07 UTC → parabole hebdomadaire"
                ;;
              "7 19 * * *")
                echo "mode=reel" >> $GITHUB_OUTPUT
                echo "category=psaume" >> $GITHUB_OUTPUT
                echo "🌙 Cron 19h07 UTC → reel psaume (Soir)"
                ;;
              *)
                echo "mode=image" >> $GITHUB_OUTPUT
                echo "category=" >> $GITHUB_OUTPUT
                echo "⚠️ Cron inattendu: $SCHEDULE"
                ;;
            esac
          else
            if [ "$MANUAL_MODE" = "auto" ]; then
              HOUR=$(date -u +%H)
              if [ "$HOUR" = "07" ] || [ "$HOUR" = "13" ] || [ "$HOUR" = "19" ]; then
                echo "mode=reel" >> $GITHUB_OUTPUT
              else
                echo "mode=image" >> $GITHUB_OUTPUT
              fi
            else
              echo "mode=$MANUAL_MODE" >> $GITHUB_OUTPUT
            fi
            echo "category=$MANUAL_CAT" >> $GITHUB_OUTPUT
            echo "🔧 Mode manuel: mode=$MANUAL_MODE category=$MANUAL_CAT"
          fi

      - name: Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL: ${{ secrets.TELEGRAM_CHANNEL }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_TOKEN: ${{ secrets.FB_PAGE_TOKEN }}
          IG_ACCOUNT_ID: ${{ secrets.IG_ACCOUNT_ID }}
          IMGBB_API_KEY: ${{ secrets.IMGBB_API_KEY }}
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          PINTEREST_ACCESS_TOKEN: ${{ secrets.PINTEREST_ACCESS_TOKEN }}
          PINTEREST_BOARD_ID: ${{ secrets.PINTEREST_BOARD_ID }}
          BOT_CATEGORY: ${{ steps.schedule.outputs.category }}
        run: |
          MODE="${{ steps.schedule.outputs.mode }}"
          if [ "$MODE" = "reel" ]; then
            python bot.py reel
          elif [ "$MODE" = "parabole" ]; then
            python bot.py parabole
          else
            python bot.py
          fi

      - name: Commit progress
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add progress.json
          git diff --staged --quiet || git commit -m "Update progress [skip ci]"
          git push || true